name: ibank_emoney
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^1.1.1
  google_fonts: ^6.3.2
  flutter_svg: ^2.2.1
  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  intl: ^0.20.2
  flutter_html: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
